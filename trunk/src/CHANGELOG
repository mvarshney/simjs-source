FCFS Facility
	Max queue length support

Performance optimizations

Entity.start() function is called at time of addEntity() 
	rather than sim.simulate()

Arguments can be passed to start() function via addEntity()
	
BUG FIX: setHistogram in TimeSeries
